$('.feedbacks__inner').slick({
    dots: false,
    infinite: true,
    speed: 500,
    fade: true,
    prevArrow: $('.feedbacks-arrows__prev'),
    nextArrow: $('.feedbacks-arrows__next'),
    cssEase: 'linear'
});